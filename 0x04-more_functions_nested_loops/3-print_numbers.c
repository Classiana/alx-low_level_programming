#include "main.h"

/**
 * main - check the code
 * Description: printing the numbers, from 0 to 9, followed by a new line.
 * Return: Always 0.
 */
void print_numbers(void);
	int n;

	for (n = 0; n <= 9; n++)
		putchar(m);
putchar('\n');
return (0);
}
